server:
  port: 8083

spring:
  application:
    name: ces-erp
  datasource:
    url: jdbc:postgresql://localhost:5434/ces_erp
    username: ces_user
    password: ces_password
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
  data:
    redis:
      host: localhost
      port: 6381
  servlet:
    multipart:
      enabled: true
      max-file-size: 50MB
      max-request-size: 50MB

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html

app:
  jwt:
    secret: ces-erp-super-secret-key-minimum-256-bit-length-required-here
    access-token-expiry: 86400000
    refresh-token-expiry: 604800000
  upload:
    dir: C:/Users/serxa/ces-uploads

logging:
  level:
    com.ces.erp: DEBUG